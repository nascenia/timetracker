= form_tag holiday_scheme_path, method: :get, class: 'row g-3' do
  .col-auto
    = select_tag :year, options_for_select(Time.current.year.downto(2018).to_a, params[:year]), class: 'form-select', prompt: 'All'
  .col-auto 
    = submit_tag 'Submit', class: 'btn btn-primary'