%h1 KPI's
%hr

= render 'search_form'

- @kpis.each do |kpi|
  %hr
  .row
    .col-md-12
      %h2= kpi.title 
    .col-md-10
      %p= kpi.description
    .col-md-2
      %label Score
      %br
      %input{ type: 'text', name: 'score', class: 'form-control kpi-score' } 
      Out of 5

