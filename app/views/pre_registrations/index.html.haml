.card 
  .card-header
    %h2.fs-5
      %span Onboarding employee: 
      %span.badge.bg-dark= @onboarding_employees.size

  .card-body
    - if @onboarding_employees.empty?
      .alert.alert-danger No Record found!
    - else
      %table.table
        %thead
          %tr
          %th Name 
          %th Email
          %th Contact number
          %th Action 
        %tbody
        - @onboarding_employees.each do |employee|
          %tr
            %td= employee.name
            %td= employee.personal_email
            %td= employee.contact_number
            %td= link_to 'Edit', pre_registration_path(employee) 
